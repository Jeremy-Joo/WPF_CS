# TekkenStats — 철권8 전적 수집/분석 (WPF)

철권8 전적을 수집하고 여러 명을 비교 리포트로 뽑는 도구.

## 구조 — 3개 프로젝트

| 프로젝트 | 타깃 | 역할 |
|---|---|---|
| `TekkenStats.Core` | net8.0 | 수집·집계 로직 |
| `TekkenStats.Cli` | net8.0 | CLI |
| `TekkenStats.Wpf` | net8.0-windows | GUI |

로직은 **`Core`에 넣는다.** Wpf/Cli 양쪽에서 쓰므로 UI 프로젝트에 로직을 직접 넣지 말 것.

## 데이터 소스 — 2개. UI에서 고른다

| 소스 | 경기 종류 | 방식 | 브라우저 |
|---|---|---|---|
| **EWGF** (`EwgfCollector`) | 퀵·랭크·방·그룹 | 페이지 HTML의 RSC flight 파싱 | **필요** (Cloudflare 수동 통과) |
| **wavu** (`WavuApiCollector`) | **랭크전만** | 공식 JSON API | 불필요 |

둘 다 `CollectResult` 를 돌려주므로 UI는 소스만 갈아끼운다. 비교 리포트도 양쪽 다 된다.

### wavu 경로 — 스크레이핑이 아니다

```
GET https://wank.wavu.wiki/player/<식별코드>/replays
Accept: application/json          ← 이 헤더가 없으면 HTML 이 온다
```

**한 번의 요청으로 그 플레이어의 전체 이력이 온다.** 페이지네이션 없음.

- **`WavuCollector`(구형, Playwright+HTML+Next 클릭)로 되돌리지 말 것.** 그게 쓰는
  HTML 페이지네이션(`?before=`)은 지금 Cloudflare 403 에 막힌다. 파일은 남아 있지만
  UI에 연결돼 있지 않다 — `WavuParser` 회귀 검증용 참조로만 둔 것이다.
- `Replay` 의 필드명은 `[JsonPropertyName]` 으로 하나씩 적혀 있다. **네이밍 정책으로
  바꾸지 말 것** — `p1_name` 처럼 숫자 낀 이름에서 조용히 어긋나고, 결과가
  '전부 null = 0경기'라 원인을 찾기 어렵다. `FetchReplaysAsync` 끝의
  `battle_at 이 하나도 없으면 예외` 검사가 이걸 잡는다. **지우지 말 것.**
- 없는 식별코드면 wavu 는 404 가 아니라 **200 + HTML 에러 페이지**를 준다(실측).
  그래서 content-type 검사가 '없는 플레이어'를 먼저 안내한다.
- 레이트리밋: "요청을 한 번에 하나씩이면 안 걸린다"(공식 문서). **여러 명 수집은
  순차다. 병렬로 바꾸지 말 것.**
- `chara_id` 는 숫자다. 매핑은 `WavuChars` — 모르는 id 는 `#숫자` 로 남긴다.
  **신캐가 추가되면 표에 `#47` 처럼 드러난다** — 그때 한 줄 추가.
  `dotnet run -- wavuapi <식별코드>` 가 미매핑 id 를 경고로 뱉는다.
- `rank`(단)도 숫자다. 사이트가 이름을 노출하지 않아 `#31` 로 둔 것은 **의도**다.
  상식으로 단 이름을 지어내지 말 것.
- 레이팅은 glicko2 = `rating_before + rating_change`(경기 **후** 값). EWGF의
  레이팅 자리(TekkenPower)와 **의미가 다르다.** 두 소스의 엑셀을 나란히 비교하지 말 것.
- 방금 끝난 경기는 `rating_before` 가 null 로 온다(사이트 표시 'TBD').
  0 으로 두면 그래프가 0 으로 꺼져서, 같은 캐릭터의 직전 레이팅을 이어받는다.
- 시각은 **KST 벽시계**로 맞춰 담는다(EWGF 경로와 같은 규약). 두 소스가 같은 날짜
  기준으로 나와야 해서다.

### wavu 엑셀 시트 — 웹과 같은 숫자여야 한다

`WavuReport` 는 시트 16개를 낸다. 이 중 7개(`flow`·`season`·`strong`·`sessions`·
`time_patterns`·`vs_rating`·`rank_history`)는 **웹의 `lib/tekken/aggregations.ts` 를
그대로 옮긴 것**이다. `Aggregations.cs` 아래쪽에 모여 있다.

**계산식을 바꾸지 말 것.** 두 저장소가 같은 데이터를 다르게 말하면 어느 쪽이 맞는지
알 수 없게 된다. 손댔으면 웹 결과와 대조해서 확인할 것 —
웹에서 `fetchReplays`→`normalizeReplays`→각 `build*` 를 돌려 JSON 으로 뽑고
엑셀 시트와 셀 단위로 맞춰보면 된다(실측 2명 × 7시트 전 셀 일치 확인함).

- 첫 시트가 `flow` 인 것은 의도다 — 파일을 열면 이게 먼저 보인다. 조회 직후 궁금한 건
  캐릭터별 누적이 아니라 "지금 상태가 어떤가 / 더 해도 되나"라서다(웹도 같은 이유로
  '흐름'이 기본 탭이다).
- `vs_rating`·`rank_history` 는 **wavu JSON 만 주는 필드**(`OppDelta`/`MyRank`)에 기댄다.
  값이 없으면 시트를 **만들지 않는다** — 전부 0인 표를 내느니 없는 게 낫다.
- `time_patterns` 는 **KST 고정**이다. 웹에는 지역을 읽어 현지 시각으로 보는 기능이
  더 있지만(`lib/wavu/region.ts`), 그건 HTML 요청이 하나 더 붙어서 여기엔 안 넣었다.
  외국 유저를 조회하면 시간대 해석이 어긋난다는 것만 알고 있을 것.

**아직 웹에만 있는 것**: 세션 조언(`advice.ts`), 지역 기반 현지 시간대, 시즌 필터 버튼
(엑셀은 눌러서 거르는 게 없어서 성격상 그대로 옮겨지지 않는다).

## 빌드 / 배포

```
dotnet build TekkenStats.slnx
publish.ps1                        # 배포

# wavu 경로는 브라우저가 필요 없어 CLI 로 그대로 검증된다 (실제 수집 1회)
cd TekkenStats.Cli && dotnet run -- wavuapi 53deQ2dmLday
```

`BUILD.md`에 빌드 절차가 따로 있다. 배포 전에 확인할 것.
`TekkenStats.Confuser.crproj` — ConfuserEx 난독화 설정.

## ⚠️ 과거에 실제로 터진 문제들

커밋 이력에 남은 것들이라 회귀시키지 말 것.

- **한글 디렉터리에서 충돌** — 경로 처리에 인코딩 가정을 넣지 말 것
- **한글 관련 충돌 오류** 별도 수정 이력 있음
- **저사양 PC에서 수집이 안 되던 문제** — 타임아웃/동시성 값을 조일 때 주의
- 식별코드 입력 시 `-`는 자동 삭제된다
- 탭 강제종료 시 자동복구 로직이 있다

## 관리자 로그로 만드는 리포트 2종

`web_game_tekken_stats_wavu` 관리자 페이지가 내보내는 접속 통계 JSON
(`tekken8stats_admin_28d_*.json`)을 입력으로 쓴다. **전적이 아니라 조회 기록**이지만,
거기 든 **식별코드 목록**이 그대로 수집기 입력이 된다는 게 요점이다.

| 리포트 | 클래스 | 수집 | 내용 |
|---|---|---|---|
| 이용 통계 | `UsageReport` | **없음** | 집중도·재방문·유입경로·조회 랭킹 |
| 인구 리포트 | `PopulationReport` | 있음 | 여러 명을 **합쳐서** 집단 분석 |

```
dotnet run -- usage <관리자JSON> [출력폴더]              # 수집 없음, 즉시
dotnet run -- population <파일> [상위N] [출력폴더]        # 상위 N명 실제 수집
```

UI에서는 **[목록 불러오기…]** 로 JSON·CSV·텍스트에서 식별코드를 뽑아 채운다
(`IdImport`). 관리자 JSON 이면 **조회수 순**으로 정렬되고 `[이용통계 엑셀]` 이 열린다.

- **`days` 를 그대로 믿지 말 것.** 파일명이 `28d` 여도 실제 데이터는 이틀뿐일 수 있다
  (사이트가 2026-08-02 배포라 그때부터만 있다). 평균을 28로 나누면 20배 틀린다.
  `AdminLog.DateRange()` 가 실제 범위를 주고, summary 시트가 둘을 나란히 보여준다.
- `PopulationReport` 는 `CompareReport` 와 목적이 다르다 — 저쪽은 2~4명 맞대결,
  이쪽은 수십 명을 합친 '이 집단은 어떤 사람들인가'. **섞지 말 것.**
- `chars` 시트가 **사람 수와 경기 수를 따로** 내는 것은 의도다. 한 명이 2만 경기를
  돌리면 경기 기준만으로는 그 사람 주캐가 '인기 캐릭터'로 보인다(실측에서 실제로 그랬다).
- 수집은 **순차 + 1초 간격**이다. 147명이면 10분쯤 걸린다. **병렬로 바꾸지 말 것**
  (wavu 레이트리밋). UI가 30명 넘으면 예상 시간을 미리 알려준다.
- 반복 수집을 줄이려면 **캐시**(`ReplayCache`)를 쓴다 — UI '설정'의 '캐시 사용(유효기간)'.
  결과폴더 아래 `.cache\<식별코드>.json` 에 **원본 Replay[] 를 저장**하고(정규화 결과가
  아니라 — 집계 규칙이 바뀌어도 재수집 없이 다시 뽑게), 유효기간 안이면 wavu 를 안 때린다.
  실측: 5명 2회차가 14.1초→6.2초(wavu 요청 0건). **wavu 전용**이다(EWGF 는 원본을 안 만든다).
  실패·빈 결과는 캐시하지 않는다 — 없는 식별코드를 저장하면 유효기간 내내 틀린 답을 준다.
  - **캐시 비우기**: UI '비우기' 버튼 또는 `dotnet run -- cache clear <결과폴더>`.
    `.json` 만 지우고 폴더는 남긴다. 147명이면 600MB 쯤 되니 참고.
  - **다른 PC 로 옮기기**: 캐시는 wavu 원본이라 기계 독립적이다. `.cache` 폴더째 복사해도
    되고, UI '내보내기/불러오기(zip)'(`CacheArchive`) 또는 `cache export/import` 로
    파일 하나로 주고받아도 된다. 실측: 3명 2.4MB → zip 0.33MB. **불러오기는 받은 시각이
    최신인 쪽을 남긴다**(오래된 걸로 덮어써 유효기간이 앞당겨지는 것을 막음).
    옮긴 캐시가 유효기간을 넘겼으면 그 PC 에서 유효기간을 넉넉히 올리면 그대로 쓴다.
- **요청 간격**은 UI '설정'에서 조절한다(`txtDelayMs`, 기본 300ms). **병렬이 아니다** —
  언제나 한 번에 하나씩이고 이 값은 요청 사이 간격일 뿐이다("하나씩이면 안 걸린다"는
  전제를 유지). 낮출수록 빠르지만 wavu 차단 위험이 커진다. 147명 순수 대기: 1초=146초,
  300ms=44초. **병렬 수집으로 바꾸지 말 것** — 429/403(Cloudflare) 위험. 첫 수집만 느리고
  재수집은 캐시로 즉시라, 병렬의 위험을 감수할 이유가 약하다.

### 인구 리포트 시트

`summary` / `players` / `chars` / `vs_chars` / `round_stats` / `season` /
`time_patterns`(플레이 시간대) 에 더해 **분포 4종**을 낸다:

- `rating_dist` — 레이팅 구간별 인원 (실력 분포)
- `wr_dist` — 승률 구간별 인원 (50% 근처에 뭉치는가, 양극단이 있는가)
- `games_dist` — 플레이 횟수 구간별 인원 (헤비/라이트 비율 — 평균이 중앙값과 벌어지는 이유)
- `rank_dist` — 최종 단(段)별 인원. **wavu JSON 만 주는 값**이라 없으면 시트를 안 만든다.

`chars` 가 **사람 수와 경기 수를 따로** 내는 것과 같은 이유로, 분포는 전부
'몇 명이 어느 구간인가'를 센다 — 경기 수로만 보면 헤비 유저 한 명이 그림을 왜곡한다.
- 텍스트/CSV 에서는 **12자 영숫자 또는 4-4-4 대시**만 식별코드로 본다.
  12자 영숫자 닉네임은 원리적으로 구분이 안 되므로(웹 `token.ts` 사고 이력),
  통과시키되 수집 단계에서 '없는 플레이어'로 드러나게 둔다.

## 주의

- 수집은 외부 사이트에 의존한다. 사이트 개편 시 조용히 깨진다.
  wavu 경로는 그 '조용히'를 막으려고 스키마 검사·content-type 검사를 넣어뒀다.

## 관련 저장소

- `web_game_tekken_stats_wavu` — 같은 wavu JSON API 를 쓰는 웹판(Next.js/Vercel).
  **wavu 관련 함정은 거기서 먼저 실측됐다** — 규모(3만 경기=15MB)·캐시·지역 추정 등.
  wavu 쪽을 손볼 일이 생기면 그 저장소의 CLAUDE.md 를 먼저 볼 것.
